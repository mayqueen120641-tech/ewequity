# EweQuity — 프로젝트 컨텍스트

매일 아침 국내외 매크로 경제지표와 경제 뉴스를 한 화면에서 확인하는 개인용 브리핑 대시보드.
2026 클로드 스터디 프로젝트 (양(Ewe) + 주식(Equity)) · 작성자: 양서현.

## 구성

- `Code.js` — 백엔드. Google Apps Script(GAS), API 5종을 프록시 + 캐싱. `clasp`로 관리.
- `index.html` — 프론트엔드. 단일 정적 HTML (목업 데이터로도 바로 열림).
  크림 톤 테마 + 좌측 사이드바. 페이지는 **해시 라우팅**(`#/dashboard` 등)으로 전환한다 —
  서버 라우팅이 없는 정적 파일이라 화면만 갈아끼운다. 페이지를 추가하려면
  ① 사이드바 `<a href="#/xxx">` ② `<section class="page" id="page-xxx">` ③ JS의 `PAGES` 배열,
  **세 곳을 같이** 고쳐야 한다.
  ⚠️ **차트는 숨겨진 동안 캔버스 폭이 0이라 제대로 안 그려진다.** 대시보드로 돌아올 때
  `showPage()`가 `renderChart()`를 다시 부른다 — 차트를 다른 페이지로 옮기면 이 처리도 옮길 것.
- `docs/` — API 발급 가이드, 배포 가이드, 진행보고서(docx).
- `README.md` — 프로젝트 개요, `SETUP_GUIDE.md` — 로컬 환경 세팅 절차 (이미 완료된 상태).

## 사용 API / 데이터 소스

| 소스 | 용도 | 키 필요 |
|---|---|---|
| 한국은행 ECOS | 기준금리(한), 원/달러 환율 | O |
| FRED | 기준금리(미), WTI 유가, 매크로 발표 일정 | O |
| Yahoo Finance (비공식) | 지수·금·비트코인 시세, 종목 검색, 관심종목 | X |
| Finnhub | 해외 대형주 실적 캘린더 | O |
| DART (금감원 오픈API) | 국내 종목 재무제표 → PER·PBR·ROE | O |
| 네이버 뉴스 검색 | 경제 뉴스 피드 | O |
| Anthropic Claude | AI 브리핑 + 뉴스 중요도 채점 + 재무지표 해설 | O (유료) |
| KIND (한국거래소) | 국내 코스피 실적 발표 일정 | X (HTML 파싱) |
| 네이버 금융 | 시가총액·등락률 순위 | X (HTML 파싱) |
| 네이버 모바일 증권 | 투자자별 수급(외국인·기관·개인) | X (비공식 JSON) |

API 키는 전부 GAS 스크립트 속성(서버 측)에만 저장 — 코드에 하드코딩 금지.

## 작업 흐름

```bash
clasp push      # Code.js 수정사항 → Apps Script 프로젝트 업로드
clasp deploy    # 웹 앱(exec URL)에 새 버전 반영 — push만으론 실제 사이트에 반영 안 됨, 꼭 deploy까지
git add . && git commit -m "설명"
git push        # GitHub(mayqueen120641-tech/ewequity) 백업
```

`index.html`은 정적 파일이라 저장 후 브라우저 새로고침만 하면 됨 (재배포 불필요).

## 알아두어야 할 과거 이슈 (재발 방지용)

- **ECOS 차단**: 한국은행이 GAS 서버 IP를 종종 차단. 이때 에러가 바로 나는 게 아니라 응답이
  올 때까지 **~50초를 매달린다**. 그래서 ECOS는 사용자 요청 경로(`doGet`)에서 완전히 뺐다 —
  아래 "ECOS 백그라운드 갱신" 참고. **ECOS 요청을 `fetchRatesParallel_`에 다시 넣지 말 것.**
- **ECOS 백그라운드 갱신**: 시간 기반 트리거가 30분마다 `refreshEcosCache()`를 실행해 ECOS
  값을 스크립트 속성 `ECOS_CACHE_V1`에 저장하고, `fetchRatesParallel_`은 `readEcosCache_()`로
  그 저장값만 읽는다. 값이 없거나 26시간(`ECOS_MAX_AGE_MS_`) 넘게 낡았으면 기준금리는
  `BASE_RATE_KR_MANUAL`, 환율은 Yahoo(`KRW=X`)로 폴백. 트리거 설치는 Apps Script 편집기에서
  **`setupTriggers()`**를 한 번 실행(중복 실행해도 안전). ECOS(30분)와 AI 브리핑(3시간)
  트리거를 함께 등록한다. 설정이 꼬였을 땐 **`checkSetup()`**을 실행하면 어떤 스크립트 속성이
  비어있는지, 트리거가 걸려있는지, 저장된 값이 있는지 로그로 알려준다.
  `BASE_RATE_KR_MANUAL`은 손으로 관리하지 말 것 — ECOS 갱신에 성공할 때마다
  `refreshEcosCache()`가 최신값으로 자동으로 덮어쓴다(방치돼 낡는 사고를 막기 위함).
- **`UrlFetchApp.fetchAll()`의 함정**: `muteHttpExceptions`는 HTTP 4xx/5xx만 막아준다.
  "Address unavailable" 같은 **연결 레벨 오류가 배치에 하나라도 있으면 fetchAll 호출 자체가
  예외를 던져 배치 전체 응답을 잃는다**. `fetchJobsSafe_()`가 배치 실패 시 개별 요청으로
  재시도해 실패를 요청 단위로 격리한다.
- **GAS CacheService 100KB 한도**: Finnhub 캘린더 데이터가 이 한도를 넘기면 요청 전체가 에러남.
  기간/필드를 트리밍하고, 캐시 저장 실패는 무시하도록 방어.
- **차트는 외부 CDN 의존 금지**: Chart.js 로드 실패 이슈 이후 Canvas 2D API 직접 구현
  (`drawDualLineChart_`)으로 전환 완료 — 다시 외부 라이브러리 추가하지 말 것.
- **스크립트 속성 캐시**: GAS가 15분간 이전 값을 반환할 수 있음. `clearAllCaches()` 유틸리티와
  `&nocache=1` 쿼리 옵션으로 우회 가능.
- **캔버스 리사이즈 그리드 버그**: `canvas.width`를 큰 픽셀값으로 재설정하면 CSS Grid가 이를
  트랙 최소 크기에 반영해 옆 패널이 찌부러짐. `.grid2 > .panel { min-width: 0; }`로 해결됨 —
  캔버스 관련 CSS 건드릴 때 주의.
- **한국 종목 검색**: Yahoo Finance 검색이 일부 한국 종목(예: 삼성전자)에서 실패할 수 있음.
  `KOREAN_TICKER_MAP_` 로컬 매핑표로 보강 + query1→query2 도메인 이중화 적용됨.
- **국내 실적은 KIND(한국거래소) IR 일정**: `kind.krx.co.kr/corpgeneral/irschedule.do`에
  **POST**로 조회한다(JSON API가 아니라 HTML 표를 파싱). `method=searchIRScheduleSub` +
  `forward=searchirschedule_sub` + `Referer` 헤더가 있어야 응답이 온다 — 하나라도 빠지면
  0바이트가 돌아온다. `marketType=1`이 코스피. GAS에서 접근 가능한 것 확인됨(ECOS와 달리
  막히지 않는다). 표는 항상 6칸 `[번호, 회사명, 내용, 장소, 날짜, 시간]`.
  같은 행사를 국문/영문 두 건으로 올리는 회사가 많아 `회사명|날짜`로 중복 제거하되
  한글 설명 쪽을 남긴다. IR 일정에는 NDR·부스 운영도 섞여 있어 실적 키워드로 걸러낸다.
- **캘린더 = FRED 매크로 + Finnhub 실적**: Finnhub는 미국 상장사 전체(하루 수백 건)를 주므로
  `MAJOR_EARNINGS_SYMBOLS_` 대형주로 좁혀서 쓴다. 예전엔 그냥 `slice(0, 60)`이라 알파벳
  앞쪽 소형주만 잡혀 "전부 같은 날짜"로 보였던 것 — Finnhub 데이터 자체 문제가 아니었다.
  FRED 발표명 매칭은 **접두사** 일치여야 한다(`majorReleaseLabel_`). 부분 일치로 하면
  "Debt to Gross Domestic Product Ratios"가 GDP 발표로 둔갑한다.
  FRED는 특정 통계가 아니라 **모든** 발표 일정을 주므로 두 달치가 한 페이지(1000건)를
  넘는다(실측 1638건). `collectFredPages_`로 남은 페이지를 병렬로 이어붙이지 않으면
  뒷달이 통째로 사라진다. `parseFredReleases_`는 응답 객체가 아니라 **행 배열**을 받는다.
  - ⚠️ **Finnhub도 한 응답에 1500건 한도가 있는데, 넘치면 `날짜가 이른 쪽`부터 잘라낸다.**
    FRED와 달리 offset 파라미터가 없어서 페이지로 못 넘긴다. 두 달치를 통째로 요청했더니
    1500건이 뒷달로 다 채워져 **7월 실적이 하나도 안 들어왔다**(아마존 7/30이 사라진 원인).
    화면엔 8월 일정이 멀쩡히 떠서 "일부만 없네" 정도로 보이는 게 함정 — 실제론 앞달 전멸이다.
    `dateChunks_`로 **주 단위로 쪼개 병렬 조회**하고, 그래도 1500건이 꽉 찬 주는 실적 시즌이라
    `collectFinnhubEarnings_`가 하루 단위로 한 번 더 쪼개 받는다(실측: 9주 + 오버플로 1주→7일
    = 16회, 2.2초). 구간이 겹칠 수 있어 `parseMajorEarnings_`가 심볼+날짜로 중복을 거른다.
    `parseMajorEarnings_`도 응답 객체가 아니라 **행 배열**을 받는다(FRED와 같은 이유).
  - 해외 실적 날짜는 **미국 현지 날짜**다. `amc`(장후)는 한국 시간으론 다음 날 아침이라
    "오늘 발표인데 어제 칸에 있다"로 보인다. 날짜를 옮기지 말 것 — 다른 모든 출처와 어긋난다.
    대신 목록에 장전/장후를 같이 띄워 구분한다.
- **AI 브리핑 (Claude API)**: `refreshAiBriefing()`이 3시간 주기 트리거로 돌면서 그날 뉴스 전체를
  Claude에 넘겨 요약 + 뉴스별 중요도(1~5)를 받아 스크립트 속성 `AI_BRIEFING_V1`에 저장하고,
  `doGet(action=briefing)`은 그 저장값만 읽는다(ECOS와 같은 이유 — 대시보드가 Claude 응답을
  기다리면 안 되므로 **요청 경로에서 호출하지 말 것**). `ANTHROPIC_API_KEY`가 없으면 조용히
  건너뛰고, 프론트는 브리핑 카드와 정렬 토글을 숨긴 채 최신순으로 동작한다.
  - **유료 API다**(클로드 코드 구독과 별개 종량제). 개인용이라 비용을 우선해 모델은
    `claude-haiku-4-5`, 주기는 **3시간**(30분 주기면 하루 48회라 비용이 확 뛴다).
    월 $2 안팎 예상. 품질을 올리려면 `claude-sonnet-5` / `claude-opus-4-8`로.
  - **모델을 올릴 때는 파라미터도 같이 바꿔야 한다.** Haiku 4.5는 구형이라
    `output_config.effort`가 **에러**이고 `thinking: {type:'adaptive'}`도 안 먹는다(4.6 이상 전용).
    지금은 사고를 끄고, 스키마의 `reason` 필드가 항목별 근거를 쓰게 해서 대신하고 있다.
    상위 모델로 올리면 `thinking: {type:'adaptive'}` + `effort`를 켤 것.
  - 응답은 **구조화된 출력**(`output_config.format`)으로 스키마를 강제한다. 스키마 제약:
    모든 object에 `additionalProperties:false`+`required` 필수, `minimum/maximum` 미지원이라
    점수 범위는 `enum: [1,2,3,4,5]`로 쓴다.
  - 적응형 사고를 켜면 **thinking 블록이 먼저** 오므로 `content[0]`을 쓰면 안 된다 —
    `type==='text'`인 블록을 찾아서 파싱할 것. `stop_reason`이 `refusal`/`max_tokens`면 저장하지 않는다.
  - 중요도는 **링크를 키로** 저장한다(카테고리 필터를 걸면 배열 인덱스가 흔들리므로).
    스크립트 속성은 값 하나당 9KB 제한이라 `reason`은 저장하지 않는다(스키마에는 남겨둠 —
    이유를 쓰게 하면 점수가 더 정확해진다). 한글은 JSON에서 문자당 6바이트로 불어나므로
    해시태그·섹터 문장도 길이를 잘라서 저장한다.
  - 한 번의 호출로 **요약 + 해시태그 + 카테고리별 한 줄 + 뉴스 중요도**를 모두 받는다.
    카테고리는 `SECTOR_KEYS_`(kospi/nasdaq/fx/oil/crypto/domestic/global) **7개 고정**이고
    스키마 `enum`으로 묶어 모델이 임의 카테고리를 만들지 못하게 한다. 한글 라벨은 프론트의
    `SECTOR_LABELS`가 붙인다 — **두 곳을 같이 고쳐야 한다.**
    모델이 key를 빠뜨리거나 중복으로 줄 수 있어 저장 시 고정 순서로 정리한다.
  - `getBriefing()`은 예전 형식(해시태그·섹터가 없던 시절)이 저장돼 있어도 빈 배열로 채워
    돌려준다. 스키마를 바꿔도 다음 트리거가 돌기 전까지 프론트가 깨지지 않게 하기 위함.
  - **초보자 모드**: 같은 호출에서 `summaryEasy`(쉬운 말 요약)와 `terms`(용어 풀이 3~4개)를
    함께 받는다. 프론트의 `쉽게 / 간결하게` 토글로 전환하며 선택은 localStorage에 기억한다.
    `summaryEasy`가 없는 예전 데이터면 조용히 `summary`로 되돌아간다.
- **뉴스 ↔ 관심종목 매칭은 클라이언트에서 한다**: 브리핑은 서버 트리거로 돌고 관심종목은
  브라우저에만 있어서 서버가 목록을 모른다. `matchedHoldings()`가 뉴스 제목·요약에서 종목명을
  찾는다 — **비용 0, 지연 0이고 관심종목이 서버로 나가지 않는다.**
  한계: 국내 종목명은 잘 맞지만 해외 종목은 한글 표기가 달라(NVIDIA vs 엔비디아) 놓친다.
- **종목 등락 원인 설명 (`action=explain`)**: 이 프로젝트의 차별점 — 증권사 앱은 "-14.7%"까지만
  보여주고 왜인지는 안 알려준다. 종목명으로 네이버 뉴스를 검색해 Claude가 원인을 설명한다.
  - 브리핑과 달리 **누를 때만 호출**한다(모든 종목을 미리 만들 수 없음). 종목별
    **30분 캐시**(`EXPLAIN_CACHE_SEC_`)로 반복 클릭 시 과금이 늘지 않게 한다.
  - **링크는 백엔드가 붙인다.** 모델에게 URL을 쓰게 하면 없는 주소를 지어낸다 — 뉴스 번호만
    받아서 실제 링크로 치환한다(브리핑 중요도 채점과 같은 패턴).
  - **개별 뉴스만 주면 "원인을 못 찾겠다"로 끝나는 날이 많다.** 실제로는 시장 전체가 빠져서
    같이 밀린 경우가 흔하기 때문. `marketContext_()`가 기존 지표·브리핑에서 시장 상황을 뽑아
    함께 넘긴다(추가 호출 없음 = 공짜). 이걸 넣고서야 "시장 흐름을 따라갔다"는 유용한 답이 나왔다.
  - 뉴스 검색은 `sort=sim`과 `sort=date`를 **둘 다** 받아 합친다. sim만 쓰면 오늘 벌어진 일이
    빠지고, date만 쓰면 이름만 겹치는 기사가 섞인다.
  - 프롬프트에 **매수/매도 추천·목표가 금지**를 명시했다. "이해를 돕는다"에 머무르는 것이
    제품 방향이고, 투자 조언은 책임 문제가 생긴다.
- **대시보드에 물어보기 (`action=ask`)**: 오늘 지표·브리핑·뉴스를 근거로 질문에 답한다.
  관심종목만 클라이언트가 `symbols`로 보내고(브라우저에만 있으므로) 나머지는 서버가 갖고 있다.
  - ⚠️ **답이 부정확하면 모델을 의심하기 전에 컨텍스트부터 볼 것.** 처음엔 지표·브리핑 요약·
    뉴스 **제목만** 넘겨서 "관심종목 어땠어?"에 시세를 몰라 추측으로 답했다.
    `buildAskContext_()`가 지표 / 브리핑 요약+섹터 / **관심종목 실제 시세**(`getQuotes`) /
    **종목 순위 급등락** / **앞으로 7일 일정** / **뉴스 본문 요약**까지 모아 넘기도록 고치니
    구체적 수치로 답하기 시작했다. 전부 이미 캐시된 값이라 추가 API 호출이 없다.
  - 기능을 새로 추가하면 **`buildAskContext_`에도 넣어야** 물어봤을 때 답할 수 있다.
  - **대화형이다.** 클라이언트가 최근 대화(`history`, 최대 4개)를 같이 보내 "그럼 왜?" 같은
    후속 질문을 알아듣는다. 대시보드 데이터는 대화 내내 같으므로 **`system`에 넣는다** —
    `messages`에 넣으면 매 턴 통째로 반복돼 토큰이 낭비된다.
    GET 쿼리로 보내므로 URL 길이 때문에 대화 수·길이를 제한한다.
  - ⚠️ **공개 URL에 AI 호출을 여는 것이라 하루 상한(`ASK_DAILY_CAP_`)을 건다.** URL이 새어나가면
    남이 질문을 반복해 크레딧을 태울 수 있다. 같은 질문은 30분 캐시로 재사용한다.
  - 프롬프트에 **매수/매도 추천·목표가 금지**를 명시하고, 그런 질문이면 `isAdvice: true`로
    표시해 화면에 안내 문구를 띄운다("결정은 본인 몫").
- **종목 기간 수익률 (`getPerf`)**: "삼성전자 한 달 전보다?" 같은 질문에 답하려면 오늘 시세만으론
  안 되고 과거 종가가 필요하다. Yahoo chart를 `range=3mo`로 **한 번만** 받아 1주/1개월/3개월을
  전부 계산한다(구간별로 따로 부르면 호출이 3배).
  - ⚠️ **인덱스로 며칠 전을 세면 안 된다.** 주말·공휴일이 빠져 있어 `points[len-30]`은 한 달 전이
    아니다. `pctChangeSince_`는 **날짜로** 기준일을 찾고, 그보다 데이터가 짧으면 가장 오래된
    점으로 대체한다.
  - `parseYahooPoints_`는 차트 히스토리(`getYahooHistory_`)와 공용이다 — 응답 형태가 같다.
  - 붙는 곳 두 군데: `buildAskContext_`(관심종목 ∪ 시총 상위 10, 중복 제거)와 `getExplain`
    (모달 시세줄 아래 "1주 · 1개월 · 3개월"). 상한가여도 한 달로는 -32%인 맥락이 같이 보인다.
  - 캐시 1시간. 심볼 조합이 키라서 `md5_`로 줄인다(순위 상위 10을 붙이면 키가 길어짐).
- **주간 리포트 (`action=weekly`)**: 브리핑은 최신 1건만 저장하므로, 주간으로 돌아보려면
  매일 따로 쌓아야 한다. `saveDailyBrief_`가 `BRIEF_DAY_YYYY-MM-DD` **속성 하나씩** 저장한다 —
  한 값에 몰아넣으면 값당 9KB 제한에 금방 걸린다. 21일 지나면 자동 삭제.
  2일치 이상 쌓여야 생성되고, 리포트 페이지를 **열 때만 한 번** 호출한다(AI 호출 절약).
- **DART 재무지표 (PER/PBR/ROE)**: DART는 PER·PBR을 **직접 주지 않는다.** 재무제표 원본만
  주므로 네이버에서 이미 긁어오는 **시가총액과 나눠서** 계산한다 —
  `PER=시총/당기순이익`, `PBR=시총/자본총계`, `ROE=당기순이익/자본총계`.
  주식수를 따로 안 구해도 되니 자기주식·우선주 보정이 통째로 빠진다.
  - ⚠️ **고유번호 매핑 정규식은 반드시 `<list>` 블록 단위로 끊을 것.** corp_code와
    stock_code를 한 정규식으로 이어 잡으면, 비상장사(stock_code가 공백)를 만났을 때
    그 회사의 corp_code에 **다음 회사의 stock_code**가 붙는다. 단위 테스트에서 SK하이닉스에
    바로 앞 비상장사 번호가 매칭되는 걸 잡았다 — **엉뚱한 회사 재무제표가 뜨는 버그라
    화면만 봐서는 절대 못 잡는다.**
  - 매핑 파일(`corpCode.xml`)은 ZIP으로 오고 풀면 20MB(11만 건)라 캐시에 못 넣는다.
    상장사만 3,900여 건 추려 `savePropChunks_`로 속성에 나눠 저장(값당 9KB 제한)하고
    **주 1회** 트리거로만 갱신한다. 조각 수가 줄었을 때 옛 조각을 지우지 않으면 다음 읽기에 섞인다.
  - 계정명은 회사마다 다르게 적힌다("당기순이익(손실)", "수익(매출액)"). 은행·보험은
    매출액이 아예 없고 **영업수익**을 쓴다. 그래서 정확히 일치가 아니라 후보 목록으로 찾는다.
  - **연결(CFS) 우선, 없으면 별도(OFS).** 지주회사를 별도로만 보면 자회사 실적이 빠져
    순이익이 실제의 몇 분의 일로 나온다.
  - 적자면 PER을 **null**로 둔다. 음수 PER을 "싸다"로 오해하기 쉽다. 자본잠식(자본총계 음수)이면
    PBR·ROE도 null. 화면에서 이유를 구분하려고 `loss` 플래그를 따로 내려준다.
  - 사업보고서 응답에는 **당기·전기·전전기가 같이** 들어 있다. 즉 호출 한 번으로 3개년
    추이가 나온다 — 연도별로 따로 부르지 말 것.
  - 사업보고서는 결산 후 90일 안에 낸다. 그래서 **4월부터** 작년 것이 있고 1~3월엔 재작년을
    봐야 한다(`latestAnnualYear_`).
  - ⚠️ **"저평가"라고 단정하지 말 것.** PER이 낮은 데는 실적이 나빠질 것이란 전망이 깔린
    경우가 많다. 화면·AI 프롬프트 모두 수치는 사실로 쓰고 판단은 사용자에게 남긴다
    (매수/매도 추천 금지 원칙의 연장).
  - **고유번호 매핑 파서는 종목코드를 앵커로** 잡고 뒤로 500자만 되짚는다. `<list>` 블록
    11만 개를 다 훑으면 요청 시간을 다 잡아먹는다(실측: 앵커 방식은 14MB를 10ms에 파싱).
    다만 **다운로드+압축해제만 3분 50초**가 걸리므로 `refreshDartCorpMap`은 요청 경로에서
    절대 부르지 말 것 — 주 1회 트리거 전용이다.
  - 스냅샷 갱신(상위 100종목)은 16초, 85종목에서 값이 나온다(나머지는 그 해 사업보고서가
    없거나 DART 고유번호가 없는 종목).
  - 국내 종목만 된다. 애플·엔비디아는 DART에 공시가 없다.
  - **화면은 `재무 분석` 페이지(`#/finance`)**. 정렬 탭(PER↓/PBR↓/ROE↑)은 서버를 다시 부르므로
    ⚠️ **요청 중 표시와 `finReqSeq` 요청 번호가 필요하다** — 없으면 이전 정렬 결과가 그대로
    남아 "탭을 눌러도 안 바뀐다"로 보인다.
  - 실적 추이 막대는 **캔버스가 아니라 div**로 그린다(숨겨진 동안 캔버스 폭이 0이 되는 문제 회피).
    적자 연도는 색을 바꿔 방향을 표시한다 — 막대 길이는 절대값이라 색이 없으면 흑자와 구분이 안 된다.
  - 삼성전자처럼 **우선주 비중이 큰 종목은 PER·PBR이 실제보다 높게** 나온다
    (네이버 시총은 보통주만, 자본총계는 전체). 화면에 주석 필요.
- **뉴스 중복**: 같은 기사가 여러 카테고리에 동시에 잡힌다(국내정세 + 국제정세 등). 백엔드
  `collectAllNews_`와 프론트 `loadNews` **양쪽 모두** 링크로 중복을 걸러야 한다.
- **종목 순위 = 네이버 금융 스크래핑**: 코스피 전 종목의 시가총액+등락률을 주는 무료 소스가
  마땅치 않다. Yahoo screener는 **crumb 인증**이 걸려 GAS에서 못 쓰고(quoteSummary와 같은 이유),
  KRX `data.krx.co.kr`은 **세션 보호**라 POST하면 `LOGOUT`만 돌아온다(쿠키를 붙여도 실패).
  그래서 `finance.naver.com/sise/sise_market_sum.naver`를 파싱한다(GAS 접근 가능 확인됨).
  - ⚠️ **이 페이지는 EUC-KR이다.** `getContentText()`를 그냥 부르면 한글이 깨진다 —
    반드시 `getContentText('EUC-KR')`.
  - 표는 한 행이 td 13칸 고정: `[0]순위 [1]종목명 [2]현재가 [3]전일비 [4]등락률 [5]액면가
    [6]시가총액(억) [7]상장주식수(천주)`. 종목 행은 `/item/main.naver?code=` 링크로 식별한다.
  - 페이지당 50종목, `MARKET_PAGES_`로 조절(현재 2 = 상위 100).
  - 상승/하락 탭은 **그 100종목을 다시 정렬한 것**이지 전체 시장 순위가 아니다.
    소형주 급등주는 안 들어온다.
- **관심종목(워치리스트)**: 목록은 **브라우저 localStorage**(`ewequity.watchlist`)에만 있다.
  서버에 두려면 공개 URL(`ANYONE_ANONYMOUS`)에 쓰기 엔드포인트를 열어야 하는데 그러면
  URL을 아는 누구나 남의 목록을 바꿀 수 있다 — **쓰기 엔드포인트를 추가하지 말 것.**
  대신 기기 간 동기화는 안 된다. 백엔드는 `action=quotes&symbols=A,B,C`로 시세만 병렬 조회
  (`getQuotes`). 워치리스트에는 검색으로 **확정된 티커만** 담기므로 `resolveSymbol_`을 거치지
  않는다. 캐시 키는 심볼이 20개면 250자 제한을 넘길 수 있어 `md5_`로 줄인다.
  `loadWatchQuotes`는 요청 번호(`watchReqSeq`)로 최신 응답만 반영한다 — 종목을 연달아
  추가하면 먼저 시작한 요청이 나중에 도착해 새 종목이 "불러오는 중"에 멈춘다.
- **달력 칸에 `aspect-ratio` 금지**: 정사각형으로 두면 패널이 전체 너비가 되는 좁은 화면에서
  칸이 94px까지 커져 달력이 화면을 다 잡아먹는다. `min-height`로 높이를 묶어둘 것.
- **미리보기 창의 file:// 제약**: 프로젝트 폴더 밖 파일은 정적 스냅샷으로 렌더링돼 일부
  fetch가 404로 실패한다(뉴스가 목업으로 보임). 백엔드 문제로 오인하지 말 것 — exec URL을
  curl로 때려보면 정상이다.
  또 **스냅샷이 옛날 JS에 멈춰 있을 때가 있다.** 새로 만든 함수가 `undefined`로 나오면
  기능 버그가 아니라 창이 낡은 것이다(`navigate`로 다른 파일을 열어도 안 바뀐다).
  확인법: 콘솔에서 `typeof 새함수`. 우회법: `fetch`로 받아 `document.write`로 갈아끼운다.
- 🔴 **예외 메시지로 API 키가 공개 URL에 유출됐던 적이 있다** (2026-08-01). `doGet`의
  `catch`가 `String(err)`를 그대로 응답에 실었는데, `UrlFetchApp` 예외에는 **실패한 요청
  URL이 통째로** 들어 있다. DART 호출이 실패하자 `crtfc_key=...`가 그대로 응답에 나왔다.
  URL만 알면 누구나 재현할 수 있었다. 지금은 `scrubSecrets_`/`scrubValue_`가 **저장된 키
  값 자체를** 응답 전체에서 찾아 `***`로 바꾼다(키 이름이 아니라 값 기준이라 어떤 경로로
  새든 막힌다). **새 외부 API를 붙이면 `SECRET_PROPS_`에 속성 이름을 반드시 추가할 것.**
  교훈: 에러 메시지는 사용자 입력만큼이나 출력을 검사해야 하는 대상이다.

### 로그인 (구글) — 보안 규칙

- 🔴 **웹앱이 `ANYONE_ANONYMOUS`다.** URL만 알면 누구나 호출한다. 따라서 **클라이언트가
  보낸 사용자 식별자를 절대 믿으면 안 된다.** `sub`를 파라미터로 받는 순간 남의 데이터를
  그대로 읽어갈 수 있다. 사용자 구분은 **검증된 토큰 안의 `sub`로만** 한다.
- **`aud` 대조는 생략 불가.** `tokeninfo`가 서명·만료를 검사해주지만, 다른 앱용으로 발급된
  토큰도 서명은 유효하다. `info.aud !== GOOGLE_CLIENT_ID`를 반드시 확인해야 아무 구글 앱
  토큰으로 들어오는 걸 막는다. `iss`·`exp`·`sub` 존재도 같이 본다.
- **비밀번호는 받지도 저장하지도 않는다.** GAS에는 bcrypt/scrypt/Argon2가 없다.
  `Utilities.computeDigest`의 SHA-256은 비밀번호용으로 부적합하다(GPU 대량 대입).
  자체 회원가입을 다시 논의하게 되면 이 점부터 꺼낼 것.
- **전화번호는 받지 않는다.** 현재 기능에 쓰이는 곳이 없다. 안 쓸 개인정보는 유출 책임만
  남는다. 알림 문자 같은 기능이 생기면 그때 별도로 받는다.
- **토큰은 POST 본문으로만 보낸다.** GET 쿼리에 실으면 접속 로그·리퍼러에 남는다.
  토큰은 유효한 동안 그 사람 자체다.
- **`Content-Type: text/plain`으로 POST할 것.** `application/json`이면 브라우저가 사전
  요청(preflight)을 보내는데 GAS 웹앱은 여기에 응답하지 못해 요청이 통째로 막힌다.
- **curl로 `doPost`를 테스트할 땐 `-X POST`를 쓰지 말 것.** GAS는 302로
  `script.googleusercontent.com`에 넘기는데, `-X POST`는 리다이렉트에도 POST를 강제해서
  405가 난다. `-d`만 쓰면 curl이 알아서 GET으로 바꾼다(브라우저도 같은 동작).
  405나 Drive 404 페이지가 나온다고 doPost가 없는 게 아니다.
- **`getProp_`는 값이 없으면 예외를 던진다.** 인증 경로에서 쓰면 내부 사정이 응답에
  실린다. 로그인 관련 속성은 `getProperty`로 직접 읽고 조용히 실패시킬 것.
- 사용자 데이터는 스프레드시트(`users`/`userdata` 시트). 스크립트 속성은 값당 9KB라
  사람이 늘면 금방 찬다. **시트 공유 설정을 절대 공개로 바꾸지 말 것** — 이메일이 들어 있다.
- 토큰은 프런트에서 `sessionStorage`에만 둔다. `localStorage`면 탭을 닫아도 남아
  공용 PC에서 다음 사람이 로그인된 상태가 된다.
- ⚠️ **구글 로그인은 https 출처에서만 동작한다.** `file://`에서는 버튼이 아예 안 뜬다.
  로컬에서 확인하려면 GitHub Pages 주소를 쓰거나 `http://localhost`를 승인 원본에 넣어야 한다.

### 투자자별 수급 (외국인·기관·개인)

- 🔴 **개인을 `-(외국인+기관)`으로 역산하지 말 것.** 기타법인·내국인이 따로 집계돼서 세 값의
  합이 0이 아니다. 삼성전자 10일 실측으로 역산값 −301만주 vs 실제 −264만주 — **37만주가
  틀린다.** `individualPureBuyQuant`를 그대로 써야 한다.
- **소스는 네이버 모바일 API 하나뿐이다** (`m.stock.naver.com/api/stock/<코드>/trend`).
  - KRX 공식 API(`data.krx.co.kr` `getJsonData.cmd`)는 세션 쿠키를 받아 붙여도 `LOGOUT`만
    돌려준다. 시도해봤자 시간만 버린다.
  - 네이버 **PC** 페이지(`finance.naver.com/item/frgn.naver`)에는 **기관·외국인만 있고
    개인이 없다.** 컬럼 이름만 보고 쓰면 안 된다.
- **최대 10거래일.** 더 긴 추이가 필요하면 매일 트리거로 쌓아야 한다(미구현).
- 코스피·코스닥 모두 되고 키가 필요 없다. 다만 **비공식 API라 언제든 막힐 수 있다** —
  `getFlow`는 실패해도 `null`을 돌려주고, `getExplain`은 `safe_`로 감싸 수급이 없어도
  설명은 나온다.
- **막대는 세 줄이 같은 배율을 써야 한다.** 줄마다 최대치를 새로 잡으면 "누가 더 많이
  샀나"가 완전히 왜곡된다. 0선을 가운데 두고 순매수는 위(빨강)·순매도는 아래(파랑).
- ⚠️ 화면·프롬프트 모두에 **"수급은 결과지 원인이 아니다"**를 명시할 것. 초보자가
  "외국인 따라 사면 된다"로 읽는 게 이 지표의 가장 흔한 오해다.

### AI에게 숫자를 설명시킬 때 (재무지표 해설)

- **여러 정보를 한 필드에 녹여달라고 하면 뒤쪽이 통째로 빠진다.** 수급을 `explanation`
  안에 넣어달라고 했더니 뉴스 설명에 밀려 한 번도 언급하지 않았다. 스키마에 `flowNote`를
  **별도 required 필드**로 빼니 그제서야 매번 나온다. 반드시 나와야 하는 정보는 필드를
  따로 만들 것.

- **지표 정의를 프롬프트에 박아넣을 것.** 안 주면 그럴듯하게 틀린다. 실제로 부채비율을
  "자산 대비"라고 설명한 적이 있는데(정답은 **자본총계 대비**), 삼성전자 기준 23% vs 29.9%로
  값 자체가 달라진다. 초보자는 검증할 수단이 없어서 그대로 믿는다.
- **모델에게 배수를 계산시키지 말 것.** 영업이익률 13.07%와 중앙값 8.07%를 두고 "5배 높다"고
  쓴 적이 있다(실제 1.62배 — 뺄셈 결과를 배수로 착각). 지금은 배수 표현을 금지하고
  "13.07%로 중앙값 8.07%보다 높습니다"처럼 **두 숫자를 나란히 놓게만** 한다.
- **"저평가"·"싸다" 같은 단어는 조건부 금지가 아니라 전면 금지.** "사야 한다고 읽히게 쓰지
  마"라고만 하면 모델이 슬그머니 쓴다. 단어 목록을 통째로 막고 "중앙값보다 낮습니다"로만
  말하게 해야 지켜진다. PER이 낮은 건 시장이 실적 악화를 예상해서인 경우가 많다.
- **비교 기준은 중앙값(평균 아님).** `dartMedians_`가 스냅샷에서 뽑는다 — 추가 호출 0.
  평균을 쓰면 PER 300배짜리 하나가 기준을 통째로 망가뜨린다.
- **해설 캐시 키에 수치를 넣을 것.** 주가가 움직이면 PER·PBR도 바뀌는데 시간으로만 만료시키면
  옛날 숫자를 설명하는 글이 남는다. `finai_<code>_<md5(year|per|pbr|roe)>`.
- 모델이 값 없는 지표(적자라 PER 없음 등)를 설명하면 **서버에서 걸러낸다** — 화면 칩에는
  `–`가 떠 있는데 해설만 있으면 앞뒤가 안 맞는다.
- 해설은 숫자보다 10~20초 늦게 온다. 그 사이 다른 종목을 열 수 있으므로 `finAiSeq`로
  마지막 요청만 그린다(`watchReqSeq`·`finReqSeq`와 같은 패턴).

## 현재 우선순위 (2026-07-28 기준)

기존 기능 신뢰도부터 잡고 신규 기능은 그다음. 순서:

1. ~~**지표 로딩 속도 개선**~~ (2026-07-28 완료) — 지표를 `fetchAll()` 병렬 요청으로 전환하고
   ECOS는 트리거 백그라운드 갱신으로 분리. 트리거 설치까지 완료돼 운영 중.
   실측 **3~5초**로 안정, 지표 8종 전부 ECOS/실데이터, 50초 튐 없음.
2. ~~**경제 캘린더 실데이터 교체**~~ (2026-07-28 완료) — FRED `releases/dates`의 매크로
   지표 발표일(CPI·고용·GDP·PCE 등) + KIND 국내 코스피 실적 + Finnhub 해외 대형주 실적.
   화면은 월 단위 달력 그리드로, 날짜를 누르면 그 날 일정이 아래에 뜬다.
   조회 범위는 이번 달~다음 달. 점 색: 주황=지표, 초록=국내 실적, 파랑=해외 실적.
3. ~~**뉴스 "중요도순" 정렬** + **AI 자동 브리핑**~~ (2026-07-29 완료) — 네이버 API엔 중요도
   개념이 없어서, Claude가 그날 헤드라인 전체를 보고 **요약과 중요도 점수를 한 번의 호출로**
   같이 만들어준다. 아래 "AI 브리핑" 항목 참고.

## 로드맵 (참고용, 아직 미착수)

- ~~관심종목(워치리스트), 시가총액 상위/급등락 순위~~ (2026-07-29 완료)
- 다크모드 등 UI 폴리싱
- 매일 아침 이메일 자동 발송
- 계좌 연동(한국투자증권 Open API, 모의투자), 모바일 앱(Flutter) — 기획서 원안 Phase 2
- (선택) GitHub Pages로 index.html 배포
